version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:17.2.0
      
    auth:
       username: frehman
       password: $DOCKERHUB_PASSWORD
    steps:
      - run:
          name: Build the Docker Image from the Dockerfile
          command: |
            docker build . --tag frehman/pipe1:v.5.55
